{
  "name": "chroma",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm -r dev",
    "dev:frontend": "pnpm -w -C apps/frontend dev",
    "dev:backend": "pnpm -w -C apps/backend dev",
    "build": "pnpm -r build",
    "build:frontend": "pnpm -w -C apps/frontend build",
    "build:backend": "pnpm -w -C apps/backend build"
  },
  "dependencies": {
    "jimp": "^1.6.0",
    "vite": "^8.0.0"
  },
  "devDependencies": {
    "@types/jimp": "^0.2.1",
    "@vitejs/plugin-react": "^6.0.1"
  }
}
